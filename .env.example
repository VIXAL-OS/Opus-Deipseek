# Discord Bot Token
# Get from: https://discord.com/developers/applications → Your App → Bot → Token
DISCORD_TOKEN=your_discord_bot_token_here

# Anthropic API Key
# Get from: https://console.anthropic.com/
# Don't forget to add credits first!
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Deepseek API Key (OPTIONAL - bot works without it, Claude-only mode)
# Get from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Gemini API Key (OPTIONAL - completes the MAGI trinity)
# Get from: https://aistudio.google.com/apikey
# Uses Google's OpenAI-compatible endpoint (no new SDK required).
GEMINI_API_KEY=your_gemini_api_key_here

# Bookclub Gemini cache TTL in hours (OPTIONAL - default 6). Gemini context
# caches bill storage per token-hour for their whole TTL, so a big idle fic cache
# is pure waste (~$11.6 for a 452k-token work at 24h vs ~$2.9 at 6h). Lower it for
# sporadic clubs, raise it for an active marathon read. Caches are deleted on
# !unload regardless; this is just the "loaded but forgotten" backstop.
GEMINI_CACHE_TTL_HOURS=6

# Fireworks API Key (OPTIONAL - enables the open-weight head GLM (Asuka), and
# Qwen too if you set providers.qwen.backend="fireworks" in config.json)
# One key, one endpoint, US zero-data-retention. Get from: https://fireworks.ai/
# Prepaid as of July 2026 — buy credits and set auto-reload so calls don't fail at $0.
# (Mistral Large 3 is NOT on Fireworks serverless — see MISTRAL_API_KEY below.)
FIREWORKS_API_KEY=your_fireworks_api_key_here

# Alibaba Model Studio (DashScope) Key (OPTIONAL - enables Qwen (Rei): Qwen 3.8
# Flash, the cheap auto-routed coder/mathematician, via the US (Virginia) region.
# ⚠️ Keys are REGION-LOCKED — create it in the US (Virginia) region of the Model
# Studio console, or dashscope-us.aliyuncs.com rejects it.
# Get from: https://modelstudio.console.alibabacloud.com/
DASHSCOPE_API_KEY=

# Mistral API Key (OPTIONAL - enables Mistral (Mari): French/EU specialist + the !french tutor)
# Mistral's own API (api.mistral.ai) — EU-resident, runs on France's low-carbon grid.
# Get from: https://console.mistral.ai/
MISTRAL_API_KEY=your_mistral_api_key_here

# Moonshot API Key (OPTIONAL - enables Kimi (Kaworu): Kimi K3, the largest
# open-weights model — 2.8T MoE, 1M context. PREMIUM pricing ($3/$15 per Mtok,
# above Claude on input), so it's override-only: !kimi / !k3 / !kaworu.
# Moonshot's own API (api.moonshot.ai) — China-resident, like DeepSeek's api.
# Get from: https://platform.kimi.ai/
MOONSHOT_API_KEY=

# Together AI Key (OPTIONAL - only for the Mistral `together` backend, Phase 3:
# US-serverless Mistral Large 3 instead of EU api.mistral.ai. Activate by setting
# providers.mistral.backend = "together" in config.json.)
# Get from: https://api.together.xyz/
TOGETHER_API_KEY=

# --- Gemini Vertex backend (OPTIONAL - Phase 2, lab/enterprise tier only) -----
# Only needed if you set providers.gemini.backend = "vertex" in config.json
# (for data residency / IAM / SLA). Auth is GCP Application Default Credentials:
# point GOOGLE_APPLICATION_CREDENTIALS at a service-account JSON with Vertex AI
# access, and `pip install google-cloud-aiplatform`. The DEFAULT developer_api
# Gemini backend needs NONE of these — leave them blank.
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=

# Tavily API Key (OPTIONAL - enables web search for Deepseek)
# Gemini uses Google's native grounding (no Tavily needed) when GEMINI_API_KEY is set.
# Get from: https://tavily.com/ (free: 1000 searches/month)
TAVILY_API_KEY=your_tavily_api_key_here

# Azure Speech (OPTIONAL - enables the !speak (Mandarin) and !french (French) TTS commands)
# The only mainstream TTS that lets the bot FORCE exact tones via SSML
# <phoneme>, which is what matters for teaching pronunciation. Free tier:
# 0.5M chars/month. In the Azure portal create a "Speech service" resource,
# then copy Key 1 into AZURE_TTS_KEY and the resource's Location/Region
# (e.g. eastus, westus2, southeastasia) into AZURE_TTS_REGION.
AZURE_TTS_KEY=
AZURE_TTS_REGION=
# Off-switch: false parks TTS without deleting the keys — no Azure calls,
# !speak / !french reply "switched off", inline clips become plain text.
# Unset or true = normal.
AZURE_TTS_ENABLED=true

# AO3 Session Cookie (OPTIONAL - for bookclub mode when AO3 is "shields-up")
# When AO3 is under heavy load it returns 403 "Shields are up!" to anonymous
# requests. Logged-in sessions bypass this AND can access registered-only works.
# To populate: log in at archiveofourown.org in your browser → open dev tools →
# Application/Storage → Cookies → archiveofourown.org → copy the value of
# `_otwarchive_session`. Paste that value here (no "Cookie:" prefix needed).
# The cookie typically lasts ~2 weeks; refresh when !load starts failing again.
AO3_COOKIE=
